#include <vector>
#include "disparo.h"
#include "ListaDisparos.h"
#include "InteraccionArena.h"
//
//void ListaDisparos::destruir_contenido() {
//    for (auto d : lista) delete d;
//    lista.clear();
//}
//
//void ListaDisparos::eliminar(int index)
//{
//    if ((index < 0) || (index >= lista.size()))return;
//    delete lista[index];
//    lista.erase(lista.begin() + index);
//}
//void ListaDisparos::eliminar(Disparo* d)
//{
//    for (int i = 0; i < lista.size(); i++)
//        if (lista[i] == d) {
//            eliminar(i);
//            return;
//        }
//}
//
//Disparo* ListaDisparos::operator[](int index) {
//    if (index >= lista.size())index = (int)(lista.size()) - 1;
//    if (index < 0)index = 0;
//    return lista[index];
//}

